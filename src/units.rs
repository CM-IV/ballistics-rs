pub enum SpeedUnit {
    FeetPerSecond,
    MetersPerSecond,
}

pub enum DistanceUnit {
    Feet,
    Meters,
}

pub enum MassUnit {
    Grains,
    Kilograms,
}

pub enum TemperatureUnit {
    Fahrenheit,
    Celsius,
}

pub enum PressureUnit {
    InchesOfMercury,
    Pascals,
}